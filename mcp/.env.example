PATENTRADAR_MCP_HOST=0.0.0.0
PATENTRADAR_MCP_PORT=8000
PATENTRADAR_PUBLIC_BASE_URL=https://mcp.yccode.xyz
PATENTRADAR_MCP_DATA_DIR=/app/data
# 使用 `uv run python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"` 生成
PATENTRADAR_MASTER_KEY=
